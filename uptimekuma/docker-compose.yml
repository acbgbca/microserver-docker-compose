---
version: "3.7"
services:
  uptimekuma:
    image: 'louislam/uptime-kuma'
    container_name: uptimekuma
    labels:
      - traefik.enable=true
      - traefik.http.routers.radarr.rule=Host(`uptime.local`)
      - traefik.http.services.radarr.loadbalancer.server.port=3001
    environment:
      - PUID=9001
      - PGID=9001
      - TZ="Australia/Melbourne"
    volumes:
      - '/ctr_cfg/uptimekuma/config:/app/data'
      - '/mnt/downloads:/tv'
      - '/mnt/videos:/media'
    ports:
      - 3001:3001
    restart: 'unless-stopped'
networks:
  default:
    external: true
    name: ctr-network