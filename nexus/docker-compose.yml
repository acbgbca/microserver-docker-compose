---
version: "3.7"
services:
  nexus:
    image: sonatype/nexus3:latest
    container_name: nexus
    user: 9001:9001
    labels:
      - traefik.enable=true
      - traefik.http.routers.nexus.rule=Host(`nexus.local`)
      - traefik.http.routers.nexus.service=svc_nexus
      - traefik.http.services.svc_nexus.loadbalancer.server.port=8081

      - traefik.http.routers.nexusdocker.rule=Host(`nexusdocker.local`)
      - traefik.http.routers.nexusdocker.service=svc_nexusdocker
      - traefik.http.services.svc_nexusdocker.loadbalancer.server.port=5000
    environment:
      - USER_UID=9001
      - USER_GID=9001
    volumes:
      - ./data:/nexus-data
      - /ctr_data/nexus/blobs:/blobs
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
    security_opt:
      - no-new-privileges:true
    restart: 'unless-stopped'
networks:
  default:
    external: true
    name: ctr-network